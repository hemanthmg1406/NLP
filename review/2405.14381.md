# Paper 2405.14381

Total equations reviewed: 7

## Equation 1

**Equation ID:** `S1.E1`

### LaTeX

```latex
\left|\,u,v,t,0^{S}\,\right\rangle\rightarrow\left|\,u,v,(t+uv)%
\text{ mod }N,0^{S}\,\right\rangle
```

### Before

To compare the amount of work performed per run, we follow Ragavan and Vaikuntanathan [28, 29], and assume that we have a quantum circuit that maps

### After

for S𝑆Sitalic_S the number of ancilla qubits, and for u,v,t∈[0,N)∩ℤ𝑢𝑣𝑡0𝑁ℤu,v,t\in[0,N)\cap\mathbb{Z}italic_u , italic_v , italic_t ∈ [ 0 , italic_N ) ∩ blackboard_Z, where we furthermore require u𝑢uitalic_u and v𝑣vitalic_v to be invertible modulo N𝑁Nitalic_N.
All of the aforementioned quantum algorithms can be implemented by calling this circuit as a subroutine.
Furthermore, for the parameterizations of the aforementioned algorithms that we consider, all other work performed in a run is negligible compared to the calls to this circuit.
Hence, we may roughly compare the amount of work that the algorithms perform by counting the number of calls that they make to this multiplication circuit.

---

## Equation 2

**Equation ID:** `S3.E2`

### LaTeX

```latex
\sum_{\bm{z}\,\in\,\{-D/2,\ldots,D/2-1\}^{d+k}}\rho_{R}(\bm{z})%
\left|\,z_{1},\ldots,z_{d+k},\prod_{i\,=\,1}^{d}a_{i}^{z_{i}+D/2}\prod_{i\,=\,%
1}^{k}x_{i}^{z_{d+i}+D/2}\text{ mod }N\,\right\rangle
```

### Before

In Regev’s algorithm [30], and Ekerå–Gärtner’s extensions [14] thereof, a state proportional to

### After

is first induced, where d𝑑ditalic_d and k𝑘kitalic_k are parameters, 𝒛=(z1,…,zd+k)𝒛subscript𝑧1…subscript𝑧𝑑𝑘\bm{z}=(z_{1},\ldots,z_{d+k})bold_italic_z = ( italic_z start_POSTSUBSCRIPT 1 end_POSTSUBSCRIPT , … , italic_z start_POSTSUBSCRIPT italic_d + italic_k end_POSTSUBSCRIPT ), and the d𝑑ditalic_d integers a1,…,adsubscript𝑎1…subscript𝑎𝑑a_{1},\ldots,a_{d}italic_a start_POSTSUBSCRIPT 1 end_POSTSUBSCRIPT , … , italic_a start_POSTSUBSCRIPT italic_d end_POSTSUBSCRIPT are small whereas the k≥0𝑘0k\geq 0italic_k ≥ 0 integers x1,…,xksubscript𝑥1…subscript𝑥𝑘x_{1},\ldots,x_{k}italic_x start_POSTSUBSCRIPT 1 end_POSTSUBSCRIPT , … , italic_x start_POSTSUBSCRIPT italic_k end_POSTSUBSCRIPT may be arbitrarily selected.
Furthermore, R=2C⁢n𝑅superscript2𝐶𝑛R=2^{C\sqrt{n}}italic_R = 2 start_POSTSUPERSCRIPT italic_C square-root start_ARG italic_n end_ARG end_POSTSUPERSCRIPT for C>0𝐶0C>0italic_C > 0 a constant, D=2⌈log⁡(2⁢d⁢R)⌉𝐷superscript22𝑑𝑅D=2^{\lceil\log(2\sqrt{d}R)\rceil}italic_D = 2 start_POSTSUPERSCRIPT ⌈ roman_log ( 2 square-root start_ARG italic_d end_ARG italic_R ) ⌉ end_POSTSUPERSCRIPT, and

---

## Equation 3

**Equation ID:** `S3.E3`

### LaTeX

```latex
\prod_{i\,=\,1}^{d}a_{i}^{z_{i}+D/2}\text{ mod }N
```

### Before

To circumvent the reversibility issue in Regev’s original arithmetic, and the space increase to which it gives rise, Ragavan and Vaikuntanathan [27, 28] have proposed to compute the product

### After

in a different way, that does not require non-invertible squarings modulo N𝑁Nitalic_N to be performed.
They accomplish this feat by decomposing the product with Fibonacci numbers in the exponent instead of powers of two, by writing

---

## Equation 4

**Equation ID:** `S3.E4`

### LaTeX

```latex
\prod_{i\,=\,1}^{d}a_{i}^{z_{i}+D/2}\text{ mod }N=\prod_{j\,=\,1}%
^{K^{(r)}}\left(\>\prod_{i\,=\,1}^{d}a_{i}^{z_{i,j}}\right)^{G_{j}^{(r)}}\text%
{ mod }N=\prod_{j\,=\,1}^{K^{(r)}}c_{j}^{G_{j}^{(r)}}\text{ mod }N
```

### Before

The product (3) is rewritten as

### After

where, for each j𝑗jitalic_j, we denote the inner product over i𝑖iitalic_i by cjsubscript𝑐𝑗c_{j}italic_c start_POSTSUBSCRIPT italic_j end_POSTSUBSCRIPT.

---

## Equation 5

**Equation ID:** `S3.E5`

### LaTeX

```latex
f(r,s)=2\cdot(3r/s+4\log s+7)
```

### Before

To compute the product (3), Ragavan introduces an additional parameter s𝑠sitalic_s that is a power of two such that r𝑟ritalic_r is a multiple of s𝑠sitalic_s.
By using 4⁢log⁡s4𝑠4\log s4 roman_log italic_s additional intermediary n𝑛nitalic_n-bit registers, Ragavan computes the product with f⁢(r,s)⋅K(r)⋅𝑓𝑟𝑠superscript𝐾𝑟f(r,s)\cdot K^{(r)}italic_f ( italic_r , italic_s ) ⋅ italic_K start_POSTSUPERSCRIPT ( italic_r ) end_POSTSUPERSCRIPT large multiplications modulo N𝑁Nitalic_N, where

### After

as described in [29, Theorem 2.4].
Note that our expression for the cost differs from the expression in [29] in that it depends directly on K(r)superscript𝐾𝑟K^{(r)}italic_K start_POSTSUPERSCRIPT ( italic_r ) end_POSTSUPERSCRIPT instead of estimating K(r)superscript𝐾𝑟K^{(r)}italic_K start_POSTSUPERSCRIPT ( italic_r ) end_POSTSUPERSCRIPT as log⁡D/log⁡β𝐷𝛽\log D/\log{\beta}roman_log italic_D / roman_log italic_β for β=(r+r2+4)/2𝛽𝑟superscript𝑟242\beta=(r+\sqrt{r^{2}+4})/2italic_β = ( italic_r + square-root start_ARG italic_r start_POSTSUPERSCRIPT 2 end_POSTSUPERSCRIPT + 4 end_ARG ) / 2.

---

## Equation 6

**Equation ID:** `S3.E6`

### LaTeX

```latex
(m+d)^{1/2}\cdot 2^{(m+d)/2}\cdot(m+1)^{1/2}\cdot T<\dfrac{\sqrt{%
2}R}{6d}\cdot(\det\mathcal{L})^{-1/m}
```

### Before

for the special case of m=d+4𝑚𝑑4m=d+4italic_m = italic_d + 4.
It can be seen that for arbitrary m𝑚mitalic_m this requirement corresponds to

### After

*No text found*

---

## Equation 7

**Equation ID:** `S3.E7`

### LaTeX

```latex
C>\dfrac{\sqrt{n}}{d}+\log{\gamma}\left(\dfrac{m+d}{\sqrt{n}}%
\right)+\dfrac{\sqrt{n}}{m}+o(1)
```

### Before

Using γ𝛾\gammaitalic_γ as determined by the heuristically estimated performance of the lattice reduction employed, the bound on C𝐶Citalic_C then becomes

### After

with κ=1+o⁢(1)𝜅1𝑜1\kappa=1+o(1)italic_κ = 1 + italic_o ( 1 ).
From this, we can see that the lower bound on C𝐶Citalic_C is minimized when m≈d≈n/log⁡γ𝑚𝑑𝑛𝛾{m\approx d\approx\sqrt{n/\log\gamma}}italic_m ≈ italic_d ≈ square-root start_ARG italic_n / roman_log italic_γ end_ARG, leading to the requirement that C>4⁢log⁡γ+o⁢(1)𝐶4𝛾𝑜1{C>4\sqrt{\log\gamma}+o(1)}italic_C > 4 square-root start_ARG roman_log italic_γ end_ARG + italic_o ( 1 ).

---

